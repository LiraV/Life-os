// Куда синхронизировать. Пока пусто — приложение работает как работало, просто
// без облака, и ни одна кнопка не врёт про синхронизацию.
//
// Как заполнить — в docs/облако.md. Оба значения открытые: они видны любому,
// кто откроет исходники сайта, и это нормально. Данные закрывает не ключ, а
// правило на стороне базы: строку человека видит только он сам.
export const CLOUD = {
  url: '',   // https://<ваш-проект>.supabase.co
  key: '',   // ключ anon (публичный)
};

export const cloudReady = () => !!(CLOUD.url && CLOUD.key);
