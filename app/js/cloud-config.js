// Куда синхронизировать. Пока пусто — приложение работает как работало, просто
// без облака, и ни одна кнопка не врёт про синхронизацию.
//
// Как заполнить — в docs/облако.md. Оба значения открытые: они видны любому,
// кто откроет исходники сайта, и это нормально. Данные закрывает не они, а
// проверка на стороне функции: она спрашивает у Яндекса, чей это токен, и
// открывает только файл того человека.
export const CLOUD = {
  api: '',        // адрес шлюза: https://<id>.apigw.yandexcloud.net
  clientId: '66cd0f94a9be420b9f56fef9d8206a5c',   // ID приложения из oauth.yandex.ru
};

export const cloudReady = () => !!(CLOUD.api && CLOUD.clientId);
